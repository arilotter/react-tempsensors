import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
  // ReactDOM.render(
  //   React.createElement(someElement),
  //   document.getElementById('mount')
  // );
});
